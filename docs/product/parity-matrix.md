# Competitive Parity Matrix

> This matrix uses **only public information** from official documentation,
> lawful user-provided exports, and observations entered by the user. It does
> not copy proprietary text, UI designs, or algorithms, and does not claim exact
> formula parity. Updated during every relevant release.

**Date verified**: 2026-07-18 (Phase 0 baseline)

## Legend

- **Explainability**: 🟢 full inputs/provenance exposed · 🟡 partial · 🔴 black-box
- **Status**: ☐ planned · 🟡 in progress · ✅ shipped

## Matrix

| # | Product area | Competitor capability | Public evidence source | Date verified | Garmin data required | In Garmin export | Via official Garmin API | Our capability | Our UI surface | Calculation method | Explainability | Status | Gap | Priority | Acceptance criteria | Legal/dependency constraint |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Recovery/readiness | Daily recovery score with strain recommendation | competitor public docs | 2026-08-03 | HRV, RHR, sleep, stress | Partial | Yes (Health API) | Versioned readiness score with factor contributions, now including a Garmin-exclusive Body Battery factor | Readiness dashboard | Deterministic weighted deviation from personal baseline (v0.2) | 🟢 | ✅ | Verified real and wired: `ReadinessController` (`/latest`, `/baseline`, `/diff`), `BaselineService`, `ReadinessScore` domain exposes algorithm version, confidence, factors (with per-factor `source`), explanation, limitations; Flutter dashboard wired to real endpoints (`_ReadinessCard`, `_ScoreDiffCard`, `_CoachCard`, `_FactorsCard`, `_DataQualityCard`, `_GarminEnrichmentCard`). **v0.2 (2026-08-03)**: added Body Battery as a real, weighted, explainable readiness factor (`W_BODY_BATTERY=0.05`, reallocated from Stress's weight) fed by the new Garmin Connect connector — deliberately not just displayed alongside the score but computed *into* it via the same personal-baseline-deviation approach as HRV/RHR, since Garmin's own app can't expose its reasoning this way. Garmin's own proprietary Training Readiness score is shown separately (`GarminEnrichmentService`) for comparison, not blended in. Unit-test verified (weight rebalance doesn't regress existing golden tests, absence doesn't penalize confidence/data-quality); not yet live-verified against the running backend as of this entry — real synced body_battery_low history already exists (14+ days), so this should activate on the next `/readiness/latest` call once the backend restarts with this code. | High | Score exposes inputs, provenance, baseline, factors, confidence, algo version, limitations | None |
| 2 | Strain | Daily & activity strain | competitor public docs | 2026-07-31 | HR zones, activity duration | Partial | Yes | Transparent internal training-load model | Training-load view | HR-zone load + TRIMP variants + ACWR | 🟢 | ✅ | Backend (`trainingload.application.TrainingLoadService`, versioned `trainingload-v1`, HR-zone-weighted, wired into the readiness ACWR factor) and Flutter UI (`/activities/training-load` — ACWR hero number with color-coded status band, acute/chronic values, 28-day trend chart, entry tile on the Activities page) both real and live-verified. Remaining polish, not blocking: no historical loadStatus-band coloring on the trend chart itself, no lap-level breakdown. | High | Load values normalized with provenance; no incomparable mixing | None |
| 3 | Sleep | Sleep stages, duration, efficiency, need | competitor and Garmin public docs | 2026-08-03 | Sleep sessions, stages | Yes | Yes | Sleep summary + personal baseline + consistency + time-in-bed | Sleep view | Stage breakdown + consistency + debt estimate | 🟢 | ✅ | `SleepInsightService` computes real stage breakdown, sleep score, honest reading-density confidence, `computeSleepConsistency()` (0-100 bed/wake-time regularity over 14 nights, noon-shifted averaging so midnight-straddling bedtimes — the common case — average correctly, unit-tested against that exact case), and the personal-baseline comparison was already surfaced (`"Xh in bed · need ~Yh"` on `_SleepScoreCard` — this matrix's prior claim that it wasn't shown was stale). Closed the one real remaining gap: `totalHours` was silently time-in-bed (deep+rem+light+**awake**) mislabeled as "total sleep" everywhere it was shown — added a distinct `asleepHours` (excludes awake) alongside it, both now shown ("Xh in bed" / "Yh actually asleep (Z%)"). Debt/plan/readiness intentionally still use `totalHours` (time-in-bed) unchanged, since the personal need baseline they compare against is itself computed the same way — switching only the actual-vs-need would silently break that comparison's internal consistency. Unit-test verified; not yet live-verified against the real backend (not restarted with this code as of this entry). | Medium | Duration, time-in-bed, stages, consistency, data-quality warnings | None |
| 4 | Sleep need | Personalized sleep need | competitor public docs | 2026-07-31 | Sleep history, baseline | Partial | No | Sleep-need estimate vs. actual, accumulated debt | Sleep view | Rolling baseline (BaselineService's real "sleep_duration" baseline, shared with the readiness score) + deficit accumulation over 14 nights | 🟢 | ✅ | Real, wired, live-verified: `GET /api/v1/sleep/summary` now includes `sleepNeedHours` (was always null before); new `GET /api/v1/sleep/debt` returns accumulated debt/surplus with nights-considered and confidence, honest empty state until enough history exists. Also fixed a real bug found while building this: `CurrentMetricsService` hardcoded a flat 7.5h "need" for every user's readiness score unconditionally (dead-code fallback to the real baseline never fired) — now empty by default so the real personal baseline is used, live-verified via a real "Sleep duration vs. need" factor appearing in `/readiness/latest` for the first time. New Sleep Debt card on the Sleep page. | Medium | Shows need, actual, deficit, sample size | None |
| 5 | Journal | Behavioral logging | competitor public docs | 2026-07-30 | User input | N/A | N/A | Journal entries (caffeine, meals, travel, alcohol, RPE, soreness) | Journal view | User-selected behaviors | 🟢 | ✅ | Verified real and wired: `JournalController` (`GET /behaviors`, `POST/GET /entries`), `JournalService` (6 categories, 33 behaviors — broader than this row's original description), entries always stored `treated_as_untrusted = true`; Flutter `journal_page.dart` wired to real endpoints with category/behavior picker and entry list. One known gap carried from the project's own record: the Flutter journal UI was verified only via `flutter analyze`/`build web`, not an interactive click-through (see `docs/qa/phase-gate-report-phase3.md` EC6). Code-level verified, not live re-verified this session. | Medium | Entries stored with provenance; treated as untrusted input | None |
| 6 | Behavioral correlations | Behavior↔outcome correlations | competitor public docs | 2026-07-30 | Journal + outcomes | N/A | N/A | Exploratory correlations with sample count & uncertainty | Correlations view | Statistical correlation with min sample size, no causation claims | 🟢 | ✅ | Verified real and wired: `CorrelationService` (group-mean comparison, min 3 samples/group required, confidence capped at "medium" — never "high", by design), `GET /api/v1/journal/correlations`, Flutter `_CorrelationsCard`. Note: "Correlations view" is not a standalone screen — it's a card embedded within the Journal page. Correlations require days with a persisted `readiness_score_history` row, so early/sparse usage will show few or none. Code-level verified, not live re-verified this session. | Medium | Sample count, effect direction, uncertainty, "not causation" caution | None |
| 7 | Daily coaching | "Why is my readiness low?" | competitor public docs | 2026-07-30 | Readiness factors | Derived | Derived | Grounded coach citing actual metrics | Coach view | LLM explains computed results; deterministic fallback | 🟢 | ✅ | Verified real and wired: `CoachController` (`GET /coach/why`), `DeterministicInsightEngine.explainReadiness()` cites actual ranked factor contributions and real day-over-day comparison when available, with an honest "no prior data" message otherwise; Flutter "Why?" button on the dashboard coach card (not a separate "Coach view" page). Fully functional without the LLM. `owi.llm.enabled` defaults to `false` — the optional Ollama-explained path exists in config but its output quality wasn't evaluated this session; deterministic fallback is what's actually verified working. Code-level verified, not live re-verified. | High | Answer cites metrics + provenance; data-quality limits explicit | Local LLM only |
| 8 | Weekly/monthly trends | Trend charts | competitor and Garmin public docs | 2026-08-03 | Time-series | Yes | Yes | Trend views with baselines, selectable window | Trends view | Rolling baselines + deviation bands | 🟢 | 🟡 | `/sleep/trends` and `/activities/trends` now accept an optional `days` query param (default 7, capped 366) instead of a hardcoded window — real now that a Garmin Connect sync can provide 200+ days of history. Flutter: 7/30/90-day `SegmentedButton` selector on both `_SleepTrendsCard`/`_ActivityTrendsCard`; beyond 7 days shows an avg/trend-direction summary above a capped 14-row recent list rather than dozens of unreadable rows. Remaining gap: (1) still no baseline/deviation-band overlay on the chart itself — the new summary row shows an avg and a simple first-half-vs-second-half direction, not a real baseline comparison; (2) no unified "Trends view" — still split across Sleep/Activities pages. Unit-test verified; not yet live-verified against a real 90-day request (backend not restarted with this code as of this entry). | Medium | Trends show baseline, deviation, data completeness | None |
| 9 | Long-term health trends | Multi-month views | Garmin public docs | 2026-08-05 | Time-series | Yes | Yes | Long-term trend views, now with real weekly/monthly rollup for activity trends | Trends view | Aggregated time-series | 🟢 | 🟡 | The hardcoded-7-day blocker from row 8 was already resolved (same `days` param). Closed the remaining rollup gap for **activity step trends**: `ActivityInsightService.computeStepTrends` now returns weekly averages beyond a 31-day window and monthly averages beyond 120 days (average steps/day across real days in the bucket, never zero-filled, never summed — summing would make bucket sizes incomparable), with a new `ActivityTrendPoint.granularity` field ("day"/"week"/"month") so the UI never shows an averaged number as if it were one real day's count. `_ActivityTrendsCard` (Flutter) discloses the granularity explicitly above the trend bars. Also fixed a real N+1 query performance issue found while doing this: the old `computeStepTrends` ran one query per date (up to 366 for `days=366`); it's now a single grouped SQL query. Unit-test verified (day/week/month bucketing, averaging not summing, DB-failure honest-empty-list). **Remaining gap, not done in this pass**: the Sleep trend view (`SleepInsightService.computeTrends`, row 3/8) has the same 366-raw-point problem and was NOT rolled up — its trend point has multiple stage fields (deep/rem/light/awake/total/score) rather than one scalar, so the same averaging approach needs more design thought before applying it; left as a distinct follow-up rather than rushed into this change. | Low | Long-term view with data-quality warnings | None |
| 10 | Activity analysis | Activity breakdown | Garmin public docs | 2026-07-29 | Activities, FIT | Yes | Yes | GET /api/v1/activities/sessions (list) and /sessions/{id} (detail) | Activities view (backend only — no Flutter UI yet) | Parsed from FIT SessionMesg (GarminFitConnector) | 🟢 | ✅ | Backend done: sport, duration, distance, avg/max speed+pace, avg/max HR, calories, HR-zone breakdown, all real and persisted. Remaining: no Flutter screen consumes these endpoints yet; lap/split-level detail (FIT LapMesg) not parsed, only whole-session summary | Medium | Activity type, duration, distance, pace, HR zones | Resolved — FIT SDK is `com.garmin:fit`, license recorded in NOTICE |
| 11 | Training recommendations | Hard/easy/rest recommendation | competitor public docs | 2026-08-03 | Readiness, training load | Derived | Derived | Recommendation with supporting factors | Coach view | Rule-based on readiness + ACWR, cites the dominant factor | 🟢 | ✅ | `DeterministicInsightEngine.buildActions()` now cites the actual dominant factor behind each recommendation (e.g. "Sleep duration vs. need (-3.0 hours) is today's biggest drag") using the same ranked-by-|contribution| approach `/coach/why` already used, rather than 3 generic strings keyed only on which score bucket the number fell into. Unit-tested: a sleep-deficit-dominated low score cites "Sleep duration vs. need" by name; a high score with a clear standout HRV factor cites it as the "strongest factor". | High | Recommendation cites factors; not medical advice | None |
| 12 | Data quality | Data-completeness warnings | General | 2026-07-31 | All | N/A | N/A | Confidence score + stale-data warnings | All data views | Completeness + freshness checks | 🟢 | ✅ | Real, wired, live-verified: `DataQualityService` computes per-metric coverage/freshness/frequency for the last 7 days from the real `measurements` table (canonical 9-metric list from the schema comment, including ones Garmin never produces today — honestly shown as 0% rather than omitted), overall completeness as the mean per-metric coverage, freshness from the single most recent reading across all metrics, `daysOfData`/`totalSources` from real `provenance`/`import_batches` joins. Verified against the real dev DB: endpoint output matched a direct SQL cross-check exactly (5 metric types present at 3/7 days coverage, 4 genuinely at 0%). | High | Every score shows confidence + data-quality level | None |
| 13 | User goals | Goal setting | General | 2026-08-04 | User input | N/A | N/A | Primary goal (identity-based, Atomic Habits framing) with real journal-behavior "votes" toward it | Profile page | User-selected from `ProfileService.SUGGESTED_GOALS`, votes computed by `JournalService.computeIdentityVotes()` | 🟢 | ✅ | This row was stale — a real goal-setting capability now exists and is wired end to end: `ProfileController` (`GET/PUT /api/v1/profile`, `GET /profile/goal-options`), `ProfileService` (display name + `primary_goal` on the `accounts` table), Flutter `profile_page.dart` (goal chips, persists via the real endpoint). Goals do influence coaching, per this row's original acceptance criteria: `JournalService.GOAL_CATEGORIES` maps each goal to its relevant journal categories, and `computeIdentityVotes()` counts how many of the account's own recent entries are "votes" toward the identity the chosen goal represents (Atomic Habits' identity-based habit framing) — shown inline on the Profile page ("Toward becoming someone who prioritizes ... counted from ..."), and the habit-cue coach feature (`/coach/habit-cue`) also reads the same goal. Not a generic "Settings" screen — goal-setting lives on the Profile page specifically. | Low | Goals influence coaching | None |
| 14 | Export | Data export | GDPR/general | 2026-07-31 | All user data | N/A | N/A | Complete local export | Privacy → Export | Full data dump | 🟢 | ✅ | Backend (`GET /api/v1/export/full`, versioned `export-v1`, every personal-data table, record counts verified against direct `psql` queries) and Flutter UI ("Export all my data" button on the Profile page, triggers a real browser file download) both real and live-verified. Home is the Profile page, not a dedicated Privacy screen — that screen doesn't exist yet (see row 15). Deletion (the matrix's other GDPR half) remains explicitly out of scope. | High | Export is complete and local | None |
| 15 | Privacy | Local-first, no telemetry | General | 2026-08-04 | N/A | N/A | N/A | Loopback-only, no hosted telemetry, rotatable token | Privacy page | Architecture-enforced | 🟢 | 🟡 | Loopback enforcement verified (application.yml/docker-compose.yml/SecurityConfig.java). Real per-request local auth (`LocalApiTokenStore`/`LocalApiTokenAuthFilter`, ADR-0008) with a real pairing screen and 401-triggered re-pairing. Privacy screen states local-first/no-telemetry facts, shows live LLM status, hosts "Export all my data" and "Delete all my data" (built earlier this session, not yet reflected in this row until now — closes the "no data-deletion capability" gap this row previously listed). **Closed this entry**: token rotation — `LocalApiTokenStore.regenerate()` + `POST /api/v1/auth/regenerate-token`, safe by construction (the endpoint is itself behind the token it's replacing, so only an already-authenticated client can call it; it returns the new token in the same response and the calling client swaps it into storage immediately — no separate re-pairing step, no lockout window for the device that triggered it). Other paired devices/tabs get 401'd and must re-pair, same as if the token file were edited by hand. Unit-tested (regenerate rotates + persists + old token immediately rejected). New "Regenerate access token" action on the Privacy page. Remaining gap: the backend `privacy` package itself is still an empty stub — the Flutter screen calls existing coach/export/auth endpoints, not a dedicated privacy module; no consent-management UI. | Critical | No health data leaves machine unless explicit | None |
| 16 | Mobile experience | iOS/Android app | competitor and Garmin public | 2026-07-18 | All | N/A | N/A | Flutter iOS/Android | Mobile app | Shared Flutter codebase | 🟢 | ☐ | Phase 4 | Medium | Mobile builds with secure storage | None |
| 17 | Watch experience | Watch app/widget | Garmin Connect IQ | 2026-07-18 | Readiness, sleep, factors | Derived | N/A | Connect IQ app | Watch | Syncs with backend | 🟢 | ☐ | Phase 5 | Low | Shows readiness, sleep, factors, sync time, data-quality warning | Connect IQ SDK |
| 18 | VO2 Max estimate | Weekly VO2max estimate with trend view | competitor public docs | 2026-08-04 | HR + pace/power during cardio activities | Yes (Garmin also estimates this) | Yes | Real VO2max estimate + monthly trend from the account's own real heart-rate data, via a published non-exercise regression, not a vendor formula | Activities view | Uth-Sorensen-Overgaard-Pedersen non-exercise regression ("Heart Rate Ratio Method", Eur J Appl Physiol 2004): VO2max ≈ 15.3 × (HRmax/HRrest); HRmax = highest recorded activity heart rate in a 180-day window, HRrest = the same personal RHR baseline the readiness score and sleep debt already use | 🟢 | ✅ | Real, unit-test verified: new `vo2max` module (`GET /api/v1/vo2max`, `GET /api/v1/vo2max/trend`) computes VO2max via the published, peer-reviewed Uth-Sorensen-Overgaard-Pedersen "Heart Rate Ratio Method" (Eur J Appl Physiol, 2004) — not a reproduction of any vendor's proprietary formula. HRmax is the highest real `max_heart_rate` recorded across the account's own FIT-parsed activity sessions in the last 180 days (a longer window than the readiness baselines' 28 days, since a physiological HRmax ceiling doesn't drift week to week the way RHR/HRV do); HRrest reuses `readiness.application.BaselineService`'s existing personal RHR baseline directly, the same figure the readiness score and sleep debt already use, rather than a second convention. Confidence is capped at "medium" (never "high"), given the formula's own documented ~10-15% margin of error, and every response cites the methodology by name/year and states plainly this is an estimate, not a lab-measured or medical VO2max. Honest empty state confirmed against the real dev database, which currently has zero rows in `activities` and no continuous `hr` measurements (only a Garmin Connect daily-wellness sync has run so far, which populates neither) — the feature correctly reports "no estimate yet" with a specific reason rather than fabricating one. A plausibility guard rejects physiologically-impossible HRmax/HRrest pairs (e.g. too small a gap) rather than computing a number from bad data. The trend endpoint computes each calendar month independently from that month's own activity/RHR data rather than a shared rolling window — a simpler, documented v1 choice, since `BaselineService` only exposes a current, not historical, baseline. Flutter: an entry card on the Activities page plus a dedicated VO2 Max view (mirroring the Training Load page) showing the estimate, the methodology in plain language, confidence/limitations, and an honest empty state. Deliberately left for later: age/sex-normalized fitness-category bands (e.g. "excellent"/"good") — the acceptance criteria only calls for estimate + trend + methodology + confidence, and a category would need profile fields (age, sex) this app doesn't yet reliably collect; a scheduled background recompute (computes on demand per request instead, like training load and sleep — see `Vo2MaxService`'s class Javadoc for the tradeoff); not live-verified against a running backend instance (unit-test and Spring Modulith module-boundary verification only). | Medium | Estimate + trend, methodology disclosed, confidence shown | None |
| 19 | Real-time stress monitoring | Continuous daytime stress score (Peak+ tier) | competitor public docs | 2026-07-27 | Continuous daytime HR/HRV | Partial | Yes | Not planned | — | — | — | ☐ | Entirely missing; requires continuous background monitoring, not just a synced session | Medium | Real-time score with factor breakdown when data present | Depends on Garmin device's continuous HR export support |
| 20 | Sleep Planner (optimal bedtime) | Nightly bedtime/wake recommendation adjusting for sleep debt, strain, circadian rhythm | competitor public docs | 2026-07-31 | Sleep history, daily strain | Partial | Partial | Real bedtime recommendation with reasoning shown | Sleep view | Inferred usual wake time (own recent pattern) + personal need + capped gradual debt repayment | 🟢 | ✅ | Real, wired, live-verified: `GET /api/v1/sleep/plan` recommends tonight's bedtime from the account's own inferred wake-time pattern (average last-reading time across recent nights, not asked for explicitly), personal need (row #4's baseline), and up to 1h/night of gradual, capped debt catch-up — never a one-night mega-catch-up. Honest empty state until both need and wake-time history exist. New Sleep Plan card leads the Sleep view. Gap: no daily-strain/training-load adjustment yet (competitor capability lists this too) — only sleep history feeds the recommendation right now. | Medium | Recommends bedtime/wake time with the reasoning shown, not just the deficit number | None |
| 21 | Women's health / menstrual cycle coaching | Cycle-phase-aware coaching across sleep/strain/stress; pregnancy insights | competitor public docs | 2026-07-27 | User-entered cycle data + physiological signals | N/A | N/A | Not planned | — | — | — | ☐ | Entirely missing from roadmap; large affected user segment | Medium-High | Opt-in cycle tracking; phase-adjusted coaching; explicit "not for contraception/conception, not medical advice" disclaimer, matching the competitor's own stated boundary | Sensitive health data — needs its own consent/privacy design pass, not just a bolt-on field |
| 22 | Blood biomarker import | Upload lab bloodwork (competitor offering: 65-marker panel via partner labs); correlates with wearable trends over time | competitor public docs | 2026-08-04 | None — external lab data, user-supplied | N/A | N/A | Real CSV import of user-supplied lab bloodwork, stored per-reading with whatever category/reference range the source row itself carried | Import → Blood biomarkers | New `biomarkers` module: stateless CSV parser (`date,biomarker_name,value,unit` + optional `reference_low`/`reference_high`/`category`) routed through the existing scan → dry-run → import pipeline (detected by header, not a separate upload path); reference-range check is a plain comparison against that reading's own supplied bounds, never an asserted "normal range" | 🟢 | ✅ | New `biomarker_readings` table (V08 migration, provenance-backed like every other imported table). `BiomarkerCsvParser` gives real per-row validation in the dry-run preview (bad dates/values reported per line, not silently dropped) and real persistence in `ImportService` — the first non-connector CSV shape that actually writes to a table instead of just being counted. `BiomarkerReferenceCatalog` seeds category + plain-language description for ~65 biomarkers relevant to a fitness/recovery context (display labeling only, sourced from general clinical-chemistry knowledge — carries no numeric range, so a name outside the catalog still imports fine with an honest blank category). Flutter `BiomarkersPage` (reachable from a discovery banner on the Import page) shows readings grouped by category with an in-range/out-of-range/no-range-provided badge per reading, a persistent "not medical advice" banner, and an expandable per-biomarker history as the trend view. `ExportService` updated so the full-export/delete-all-data guarantee (row 14) covers this new table too. Unit-test verified (parser, reference catalog, repository, ingestion wiring); not live-verified against the running backend. **Deliberately deferred**: PDF import (acceptance criteria below mentions it, but lab PDF formats vary too much to parse reliably without real-world samples, which don't exist in this project — CSV covers the "no lab-partner requirement" part of the ask); an actual chart widget for the trend view (a sorted list, consistent with how every other trend view in this app renders today — no chart library is wired up anywhere yet). | High | Manual import of lab PDF/CSV; biomarkers plotted alongside relevant metric trends; explicitly no diagnosis language, no lab-partner requirement (unlike the competitor offering, which requires their partner labs) | None — pure user-uploaded local file, no vendor API dependency |
| 23 | Medical-grade monitoring (ECG / AFib / blood pressure) | On-demand ECG (FDA-cleared), AFib detection, daily BP estimate — top hardware/membership tier only | competitor public docs | 2026-07-27 | Requires the competitor's proprietary medical-grade hardware sensors | No | Partial (some Garmin models have region-limited ECG) | Not planned | — | — | — | ☐ | Hardware-blocked, not a software gap | Low | N/A | Requires FDA clearance and sensor hardware Garmin doesn't expose via export/API — infeasible without a new hardware partnership |
| 24 | Healthspan / biological age score | Composite biological-age and "pace of aging" score from 9 metrics (VO2max, RHR, sleep consistency, strength activity, etc.); 6-month and 30-day windows | competitor public docs (methodology developed with an external aging-research institute) | 2026-07-27 | 30-180 days of rolling history across sleep/activity/fitness | Yes | Yes | Not planned | — | — | — | ☐ | Feasible with existing Garmin data once enough history accumulates; no special hardware needed | Medium | Composite score with full factor breakdown, versioned methodology, explicit "wellness estimate, not a medical or actuarial age" disclaimer | Needs a long-term data-retention design (6mo+ rolling windows) |
| 25 | Strength training tracking | Log sets/reps/weight; Strength Activity Time trend view (weekly/monthly/6mo) + goal-setting | competitor public docs | 2026-08-04 | Manual entry or Garmin strength activities | Yes | Yes | Real manual set/rep/weight logging + a "Strength Activity Time" trend combining that with real Garmin-derived session duration, disclosed separately; optional weekly-minutes goal | Activities → Strength training / Log workout | New `strength` module: `strength_workouts`/`strength_sets` tables (V09 migration) for manual logs; trend buckets `activities` rows with `sport = 'training'` (the closest FIT signal — sub-sport isn't parsed) alongside manual workouts, weekly=daily/monthly=weekly/sixmonth=monthly buckets; a manual workout without a user-given duration is estimated at 3 min/set (stated formula, not measured) | 🟢 | ✅ | Real, unit-tested, not live-verified against a running backend. `StrengthWorkoutRepository` persists free-text exercise name + reps + optional weight per set (same "curated, not enforced" convention as journal behaviors/biomarker names), linked to the account with a real `started_at` timestamp. `StrengthTrainingService` computes the trend from **two disclosed sources**: real Garmin session duration (`activities.duration_seconds` where `sport = 'training'`) and manually-logged duration (user-reported `duration_minutes`, or — when absent — `totalSets * 3` minutes, an openly-stated rule of thumb mirroring `TrainingLoadService`'s HR-zone fallback, never silently presented as measured). Every trend point (`GET /api/v1/strength/trends?window=weekly\|monthly\|sixmonth`) reports `garminMinutes`/`manualMinutes`/`totalMinutes` separately plus a `manualMinutesEstimated` flag — the two sources are never blended into one unlabeled number. Optional goal: a single `accounts.weekly_strength_minutes_goal` column (`GET`/`PUT /api/v1/strength/goal`) rather than a new `ProfileService.SUGGESTED_GOALS` entry — that list's existing "Strength & training load" option is a general coaching-emphasis choice already wired into journal identity votes, not a numeric weekly target, so this is a deliberately separate, minimal field. `strength_workouts`/`strength_sets` are included in the full export and delete-all-data (row 14 guarantee). Flutter `StrengthLogPage` (add exercises/sets, optional duration/note) and `StrengthTrendPage` (weekly/monthly/6mo `SegmentedButton`, same convention as the training-load/activity trend views; stacked Garmin-vs-manual bar; goal card; `ConfidenceBanner` for the disclosed limitations) reachable from a new entry-point card on the Activities page. Unit tests cover the repository (batch-insert field mapping, bodyweight sets stored as `null` not `0`) and the service (duration-estimate formula, Garmin+manual bucket combination, ISO-week bucketing, DB-error fallback). **Deliberately deferred, same reasoning as row 22's PDF-import deferral**: parsing Garmin FIT's set-level strength messages (e.g. `SetMesg` per-set reps/weight) — real, separate, larger work; only the already-parsed session-level sport/duration feeds the Garmin side of the trend. **Known limitation, disclosed in the API response and UI**: matching Garmin sessions by `sport = 'training'` is a heuristic (verified against the FIT SDK's `Sport`/`SubSport` enums — there is no dedicated top-level "strength" sport, and this app doesn't parse `SubSport.STRENGTH_TRAINING` yet), so it can include other training types or miss ones tagged differently. Trend bars are the same custom `LinearProgressIndicator`/`Row`-flex style every other trend view in this app uses — no chart library wired up anywhere yet. | Medium | Parses strength activities from Garmin export/FIT; shows trend + optional goal | None |
| 26 | AI workout generation | Generate a custom workout from stated goals/equipment/limitations | competitor public docs | 2026-07-27 | N/A (LLM-generated) | N/A | N/A | Not planned | — | — | — | ☐ | Natural fit for the existing local-LLM coach module | Low-Medium | LLM-generated suggestion clearly labeled as such, not a prescription; deterministic template fallback when LLM disabled, consistent with core principle #2 | Local LLM only, per core principle #1 |
| 27 | Journal behavior-library breadth | 140+ trackable behaviors across 6 categories (Hormonal Health, Nutrition, Mental Wellbeing, Sleep, Supplements, Recovery), ~300 available to choose from; correlation requires ≥5 yes/5 no in 90 days | competitor public docs | 2026-07-27 | User input | N/A | N/A | Basic journal planned (caffeine, meals, travel, alcohol, RPE, soreness) | Journal view | User-selected | 🟢 | ☐ | Existing row #5 is far narrower in scope than what's actually expected here | Medium | Expand the behavior taxonomy toward this breadth; user chooses which to track; same min-sample-size discipline already specified in row #6 | None |
| 28 | Social / Teams | Create/join teams with friends for group activity comparison | competitor public docs | 2026-07-27 | N/A | N/A | N/A | Not planned | — | — | — | ☐ | Structurally conflicts with single-user local-first architecture unless explicitly opt-in hosted | Low | Only feasible under an opt-in hosted multi-user mode; must never be default-on | Requires Phase 6 hosted backend; violates core principle #1 if not strictly opt-in |
| 29 | Third-party ecosystem sync (Strava / Apple Health / TrainingPeaks) | Bi-directional sync of activities and vitals with other fitness/wellness apps | competitor public docs | 2026-07-27 | N/A | N/A | N/A | Not planned | — | — | — | ☐ | High retention value, low privacy risk if export-only and opt-in | Medium | Optional, explicit per-service opt-in export; no import of third-party "insights" as if they were our own computed scores | User must explicitly authorize each integration individually |
| 30 | Monthly/longitudinal performance assessment | Structured monthly report requiring 28 recovery scores; strain/sleep/recovery breakdown over the period | competitor public docs | 2026-07-27 | 28+ days of recovery scores | Yes | Yes | Not planned | — | — | — | ☐ | Natural extension of the Trends rows (#8, #9) once baseline history accumulates | Low-Medium | Auto-generated monthly summary once sufficient history exists; explicit "insufficient history" state before then | None |

## Notes

- This matrix is a living document. Update during every relevant release.
- "Our capability" describes original methodology, not vendor parity.
- Explainability is a core differentiator: every score exposes inputs and provenance.
- No proprietary formula, UI, or text is copied.