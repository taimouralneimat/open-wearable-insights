package com.openwearableinsights.api.workoutgen.domain;

/** The three blocks every generated workout is structured into, in order. */
public enum ExerciseCategory {
    WARMUP,
    MAIN,
    COOLDOWN
}
