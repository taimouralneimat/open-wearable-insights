/**
 * Named interface: identity domain types.
 *
 * <p>Exposed so other modules (e.g. journal) may read {@code Profile}
 * fields returned from {@code ProfileService} without reaching into
 * internal packages.
 */
@org.springframework.modulith.NamedInterface("domain")
package com.openwearableinsights.api.identity.domain;
