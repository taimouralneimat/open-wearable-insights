/**
 * Named interface: activities domain types.
 *
 * <p>Exposed so other modules (e.g. milestones) may depend on {@code
 * ActivityTrendPoint} without reaching into internal application/adapter
 * packages. Mirrors the pattern used by {@code readiness.domain.package-info}.
 */
@org.springframework.modulith.NamedInterface("domain")
package com.openwearableinsights.api.activities.domain;
